# --- Default: auto-detect text files and normalize line endings ---
* text=auto

# --- Force LF on code/config files (works best on Vercel/Linux) ---
*.sh      text eol=lf
*.js      text eol=lf
*.ts      text eol=lf
*.css     text eol=lf
*.scss    text eol=lf
*.html    text eol=lf
*.json    text eol=lf
*.md      text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.xml     text eol=lf
*.gradle  text eol=lf
gradlew   text eol=lf

# --- Windows batch scripts should stay CRLF ---
*.bat     text eol=crlf

# --- Lock files: keep stable LF so diffs are clean ---
package-lock.json text eol=lf
pnpm-lock.yaml    text eol=lf
yarn.lock         text eol=lf

# --- Treat these as binary to avoid pointless diffs ---
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.ttf   binary
*.otf   binary
*.woff  binary
*.woff2 binary
*.mp4   binary
*.mov   binary
*.pdf   binary

# Android / Java artifacts that might sneak in (still ignored via .gitignore)
*.apk   binary
*.aab   binary
*.jar   binary
*.dex   binary
*.keystore binary

# --- GitHub linguist tweaks (don’t let assets skew language stats) ---
sitefrontend/icons/* linguist-vendored